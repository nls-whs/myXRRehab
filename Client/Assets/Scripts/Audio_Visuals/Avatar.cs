using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Avatar : MonoBehaviour
{

   /* public GameObject pelvis;

    public GameObject commManager;
    private CommunicationManager cm;

    // Start is called before the first frame update
    void Start()
    {
        cm = commManager.GetComponent<CommunicationManager>();
        this.gameObject.SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        if (this.gameObject.activeSelf)
        {            
            pelvis.transform.position = new Vector3(cm.GetJointPositionVector("Pelvis").x, -cm.GetJointPositionVector("Pelvis").y + 0.20f, cm.GetJointPositionVector("Pelvis").z);
        }
       
    }*/
}
